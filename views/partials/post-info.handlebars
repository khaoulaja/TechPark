<div class=" card post">
  <div class="card-header title">
    <h3 class="text-capitalize"><a href="/post/{{id}}"> {{title}} </a><span class="subtitle">Posted by {{user.username}} on {{format_date created_at}}</span></h3>
  </div>
  <div class="card-body meta">
     
    <p>{{content}}</p>
    |
    <a href="/post/{{id}}">{{comments.length}} {{ format_plural "comment" comments.length}}</a>
  </div>
</div>